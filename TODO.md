# Recoverse TODO

이 문서는 `PRODUCT_SPEC.ko.md`, `DESIGN_SYSTEM.ko.md`, `ARCHITECTURE.ko.md`를 기준으로 한 작업 목록이다.

## 작업 원칙

- 한 번에 작은 단위만 수정한다.
- 작업 하나가 끝날 때마다 테스트 가능한 상태로 만들고 커밋한다.
- MVP는 localStorage 기반으로 유지한다.
- 로그인, 클라우드 저장, PDF 내보내기, 공유 링크는 현재 구현 범위에서 제외한다.
- 화면 문구와 기본 질문은 한국어를 우선 지원하고, 제품 설정에서 한국어/영어 선택을 유지한다.
- 문서와 README를 작성하거나 수정할 때는 한글판과 영어판을 함께 관리한다.

## 현재 완료된 기반 작업

- [x] 캡슐 중심 데이터 모델과 localStorage 저장 구조 추가
- [x] 기존 연도별 기록을 연도 회고 캡슐로 변환
- [x] 캡슐 홈, 캡슐 생성, 질문 카드 편집 기능 추가
- [x] 캡슐 JSON 내보내기/가져오기 추가
- [x] 캡슐과 질문 카드 삭제 기능 추가
- [x] 캡슐 검색/필터 기능 추가
- [x] 질문별 비교 화면 추가
- [x] 다시 발견하기 카드에서 해당 질문 카드로 이동
- [x] 캡슐 상세 진행률/빈 상태 추가
- [x] 캡슐 import/export와 legacy conversion 테스트 추가
- [x] 캡슐 MVP README 작성
- [x] 제품/디자인/아키텍처 문서 한글판과 영어판 작성
- [x] 제품 언어 선택 설정 추가
- [x] `LanguageSelector`를 `App.vue`에서 분리

## 1단계: App.vue 안전 분리

목표: 기능 변경 없이 `App.vue`의 UI 덩어리를 작은 컴포넌트로 분리한다.

- [x] `CapsuleToolbar` 분리: 캡슐 JSON 내보내기, 가져오기, 새로고침 버튼
- [x] `CapsuleCreateForm` 분리: 캡슐 제목, 설명, 유형, 기본 질문 템플릿 입력
- [x] `CapsuleList` 분리: 캡슐 검색 결과 목록과 빈 상태
- [x] `RediscoverCard` 분리: 다시 발견하기 카드
- [x] `CapsuleDetailEditor` 분리: 선택된 캡슐의 질문 카드와 답변 편집 영역

## 2단계: 화면 구조 정리

목표: 제품 명세의 핵심 화면인 홈과 캡슐 상세를 코드 구조에서도 분리한다.

- [x] `views/HomeView.vue` 생성
- [x] `views/CapsuleDetailView.vue` 생성
- [x] 홈 화면에 다시 발견하기 카드와 내 캡슐 목록을 우선 배치
- [x] 모바일 우선 레이아웃을 기준으로 PC에서는 목록/상세를 나란히 표시
- [x] 기존 연도 보기와 질문 비교 화면의 위치를 재검토하고 캡슐 중심 메뉴로 정리

## 3단계: 데이터/도메인 구조 분리

목표: 공유, 백업, 템플릿 확장을 고려해 도메인 로직을 UI에서 분리한다.

- [x] `types/recoverse.ts`로 Capsule, CapsuleCard, Backup 타입 이동
- [x] `lib/capsuleTemplates.ts`로 기본 질문 템플릿 이동
- [x] `lib/capsuleImportExport.ts`로 JSON import/export 로직 이동
- [x] `recoverseStore.ts`는 저장소 읽기/쓰기와 migration 중심으로 축소
- [x] import 실패, 중복 데이터, 버전 불일치 케이스 테스트 보강

## 4단계: 디자인 시스템 적용

목표: 타임캡슐, 종이 편지, 개인 아카이브 감성을 일관된 UI로 만든다.

- [x] 색상 토큰 정리: 따뜻한 배경, 종이 느낌 표면, 잉크 계열 텍스트
- [x] 카드, 버튼, 입력, 배지, 진행률의 공통 스타일 정리
- [x] Home 와이어프레임 기준으로 모바일 첫 화면 재구성
- [x] PC 화면에서 목록과 상세 영역의 정보 밀도 조정
- [x] 귀여운 캐릭터 요소는 보조 장식으로만 사용할 수 있는 위치 정의

## 5단계: 기록 경험 개선

목표: 기록을 쉽게 남기고 나중에 잊은 기억을 다시 발견하는 경험을 강화한다.

- [x] 새 질문 카드 추가 흐름 단순화
- [x] 답변 저장 피드백 개선
- [x] 최근 수정한 질문 카드 표시
- [x] 미답변 질문 카드만 보기 필터 추가
- [x] 다시 발견하기 카드의 선택 기준 개선

## 6단계: 백업과 가져오기 개선

목표: 사용자가 내려받았던 기록을 안전하게 다시 가져올 수 있게 한다.

- [x] 백업 파일 버전 표시
- [x] 가져오기 전 미리보기: 추가될 캡슐 수, 질문 카드 수, 중복 수 표시
- [x] 가져오기 후 결과 메시지 개선
- [x] 잘못된 JSON 파일과 지원하지 않는 버전의 오류 메시지 개선
- [x] 기존 연도별 백업 파일 import 경로 유지 확인

## 7단계: 후순위 확장 준비

현재 구현하지 않지만 구조가 막히지 않도록 준비한다.

- [x] 공유용 읽기 전용 데이터 모델 초안 작성
- [x] 비밀번호 링크 공유에 필요한 필드 후보 정리
- [x] 로그인/클라우드 저장 도입 시 localStorage migration 전략 정리
- [x] Markdown/PDF 내보내기 데이터 포맷 초안 작성

## 8단계: 우주 아카이브 홈 리디자인 준비

목표: 선택한 1안 와이어프레임을 기준으로 홈 화면을 `우주 아카이브 + 기억 행성 지도` 구조로 바꾸기 전에 데이터와 컴포넌트 경계를 안정화한다.

- [x] 홈에서 필요한 파생 데이터 정리
  - 난이도: 중
  - 위험도: 중
  - 내용: 캡슐별 질문 수, 답변 수, 최근 수정일, 오늘의 발견 대상, 캡슐 타입별 표시 정보를 계산한다.
- [x] `HomePage` 역할을 명확히 분리
  - 난이도: 중
  - 위험도: 낮음
  - 내용: 홈 화면의 데이터 연결과 이벤트 처리를 상위 화면 책임으로 모은다.
- [x] `HomeHeader` 컴포넌트 분리
  - 난이도: 하
  - 위험도: 낮음
  - 내용: 브랜드, `나의 기억 우주`, 언어/설정 진입점을 담당하게 한다.
- [ ] `DiscoveryCard`를 새 디자인 책임에 맞게 정리
  - 난이도: 중
  - 위험도: 낮음
  - 내용: 오늘의 발견, 오래된 답변 일부, 다시 열어보기 액션을 담당하게 한다.
- [ ] `CapsuleListSection`과 `CapsuleListItem` 분리
  - 난이도: 중
  - 위험도: 낮음
  - 내용: 지도 아래의 내 캡슐 목록을 보조 탐색 영역으로 분리한다.

## 9단계: 기억 행성 지도 구현

목표: Three.js 없이 Vue3와 CSS만으로 홈의 핵심 시각 요소인 행성 지도와 행성 캡슐을 구현한다.

- [ ] `GalaxyMap` 뼈대 구현
  - 난이도: 중
  - 위험도: 중
  - 내용: 지도 컨테이너, 모바일 우선 높이, 빈 상태, 캡슐 배치 영역을 만든다.
- [ ] `GalaxyStars` 장식 레이어 구현
  - 난이도: 하
  - 위험도: 낮음
  - 내용: CSS 기반 별 배경과 낮은 강도의 twinkle 효과를 구현한다.
- [ ] `CapsulePlanetCard` 구현
  - 난이도: 중상
  - 위험도: 중
  - 내용: 캡슐을 행성 카드로 표현하고 타입별 색상, 진행 상태, 최근 수정 표시를 반영한다.
- [ ] `CreatePlanetButton` 구현
  - 난이도: 하
  - 위험도: 낮음
  - 내용: `새 기억 행성 만들기` 진입점을 지도 안에 배치한다.
- [ ] 캡슐 수 증가 시 행성 배치 규칙 정리
  - 난이도: 중
  - 위험도: 중
  - 내용: 모바일에서 행성이 겹치지 않도록 고정 슬롯 또는 계산된 위치 규칙을 적용한다.

## 10단계: 홈 경험 연결과 전환

목표: 우주 지도 홈이 실제 캡슐 탐색 흐름과 자연스럽게 연결되도록 한다.

- [ ] 오늘의 발견에서 해당 캡슐/카드로 이동 연결
  - 난이도: 중
  - 위험도: 낮음
  - 내용: 발견 카드 클릭 시 기존 카드 편집 흐름으로 이동한다.
- [ ] 행성 클릭 시 캡슐 상세로 이동
  - 난이도: 중
  - 위험도: 중
  - 내용: `CapsulePlanetCard` 선택 이벤트를 기존 상세 화면 상태와 연결한다.
- [ ] 행성 클릭 확대 애니메이션 추가
  - 난이도: 중상
  - 위험도: 중상
  - 내용: CSS scale 전환 후 상세 화면으로 넘어가는 흐름을 구현한다.
- [ ] `prefers-reduced-motion` 대응
  - 난이도: 하
  - 위험도: 낮음
  - 내용: 부유, 반짝임, 확대 애니메이션을 줄일 수 있게 한다.

## 11단계: 캡슐 상세 화면 시각 개편

목표: 홈의 우주 아카이브 경험이 캡슐 상세에서도 끊기지 않도록 한다.

- [ ] `CapsuleHeroPlanet` 설계 및 구현
  - 난이도: 중
  - 위험도: 중
  - 내용: 선택된 캡슐을 큰 행성 비주얼과 제목, 진행률로 보여준다.
- [ ] `CapsuleSummary` 정보 구조 정리
  - 난이도: 하
  - 위험도: 낮음
  - 내용: 캡슐 설명, 유형, 최근 수정일, 질문/답변 수를 정리한다.
- [ ] 질문 카드 용어를 `Memory Card` 방향에 맞게 조정
  - 난이도: 중
  - 위험도: 중
  - 내용: 기존 기능을 유지하면서 화면 문구와 컴포넌트 책임을 기억 카드 중심으로 정리한다.
- [ ] 답변 작성 영역의 가독성 유지 확인
  - 난이도: 중
  - 위험도: 중
  - 내용: 감성적 비주얼 안에서도 긴 텍스트 작성이 불편하지 않게 한다.

## 12단계: 모바일/PC 마감 검증

목표: 모바일 우선 홈이 실제 화면 크기에서 깨지지 않고, PC에서도 정보 밀도가 적절한지 확인한다.

- [ ] 모바일 첫 화면 검증
  - 난이도: 중
  - 위험도: 중
  - 내용: 상단 브랜드, 오늘의 발견, 기억 지도 시작 부분이 한 화면에 자연스럽게 보이는지 확인한다.
- [ ] PC 레이아웃 검증
  - 난이도: 중
  - 위험도: 중
  - 내용: 지도 영역과 보조 목록/액션 영역의 비율을 조정한다.
- [ ] 색상 대비와 텍스트 겹침 확인
  - 난이도: 중
  - 위험도: 중
  - 내용: 행성 카드 라벨, 버튼, 답변 텍스트가 모든 뷰포트에서 읽히는지 확인한다.
- [ ] 테스트와 빌드 검증
  - 난이도: 하
  - 위험도: 낮음
  - 내용: 기존 store 테스트, 타입 체크, production build를 통과시킨다.

## 현재 제외 범위

- [ ] 로그인 구현
- [ ] 클라우드 저장 구현
- [ ] 친구 공개/공유 링크 구현
- [ ] PDF 내보내기 구현
- [ ] 실시간 동기화 구현
